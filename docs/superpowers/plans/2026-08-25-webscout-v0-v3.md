# WebScout V0→V3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Xây WebScout — mini deep-research agent với vòng lặp Research → Verify → Research if necessary → Answer, qua OpenRouter, trace LangSmith, có skill và evals.

**Architecture:** Deep Agents là node `research` bên trong graph LangGraph 3 node (research/verify/answer). Search dùng server tool `openrouter:web_search` đính vào request của model; fetch là function tool httpx+trafilatura. Verifier/Answer là bounded structured calls.

**Tech Stack:** Python ≥3.12, uv, deepagents, langchain-openai, langgraph, trafilatura, httpx, pydantic-settings, langsmith, pytest.

**Spec:** `docs/superpowers/specs/2026-08-25-webscout-design.md` — executor phải đọc spec song song plan này.

## Global Constraints

- Python >= 3.12; quản lý dependency bằng `uv`; chạy test bằng `uv run pytest`.
- Mọi model call đi qua OpenRouter `base_url = "https://openrouter.ai/api/v1"`.
- Model mặc định mọi role: `stealth/ox-alpha`. Temperature: researcher 0.2, verifier 0.0, answer 0.3.
- `MAX_ITERATIONS = 3`. Search params mặc định: `max_results=5, max_uses=4, max_characters=4000`. Fetch: timeout 15s, cap 20.000 ký tự.
- Answer theo ngôn ngữ của câu hỏi.
- Nguồn chính thức (`sources`) CHỈ lấy từ annotations `url_citation` — không tin URL agent tự gõ; URL lạ vào `weak_claims`.
- `.env` không bao giờ commit. Test integration (`@pytest.mark.integration`) tự skip khi thiếu `OPENROUTER_API_KEY`, cần key thật để chạy.
- Commit sau mỗi task, kiểu conventional commits (`feat:`, `test:`, `docs:`, `chore:`).
- Shell là Windows PowerShell; chain lệnh bằng `;` hoặc script riêng, KHÔNG dùng `&&`.
- Code không thêm comment trừ khi bắt buộc để giải thích logic không tự rõ.
- Lệch so với spec được ghi nhận (đã duyệt trong quá trình viết plan): (1) state thêm trường `search_calls: int` phục vụ metrics V3; (2) helper parse nằm ở `app/nodes/parsing.py` thay vì nhét vào research.py — dùng chung bởi node và CLI; (3) verify node ép JSON qua prompt + Pydantic validate thay vì `with_structured_output` (tương thích mọi model, test offline deterministic); trường `answer_language` của spec giữ nguyên trong state.

## Milestone mapping

| Task | Đóng góp mốc |
|---|---|
| 1–8 | Chuẩn bị + spike M0 |
| 9 (+10 acceptance) | **V0** |
| 11–15 (+16 acceptance) | **V1** |
| 17 | **V2** |
| 18–19 | **V3** |
| 20 | Hoàn thiện |

---

### Task 1: Scaffold dự án + cấu hình

**Files:**
- Create: `pyproject.toml`, `.gitignore`, `.env.example`, `config.yaml`
- Create: `app/__init__.py`, `app/config.py`
- Create: `tests/__init__.py`, `tests/test_config.py`

**Interfaces:**
- Produces: `get_settings() -> Settings` (cached); các dataclass config: `RoleConfig(model:str, temperature:float)`, `SearchConfig(max_results:int, max_uses:int, max_characters:int)`, `FetchConfig(timeout_seconds:float, max_chars:int, user_agent:str)`, `Settings` có fields: `openrouter_api_key:str`, `openrouter_base_url:str`, `researcher/verifier/answer: RoleConfig`, `max_iterations:int`, `skills_enabled:bool`, `search:SearchConfig`, `fetch:FetchConfig`.

- [ ] **Step 1: Tạo pyproject.toml**

```toml
[project]
name = "webscout"
version = "0.1.0"
description = "Mini deep-research agent"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.6",
    "langchain-openai>=1.0",
    "langgraph>=1.0",
    "trafilatura>=2.0",
    "httpx>=0.28",
    "pydantic-settings[yaml]>=2.6",
    "langsmith>=0.3",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
webscout = "app.main:main"

[tool.pytest.ini_options]
markers = [
    "integration: requires OPENROUTER_API_KEY and network",
]
```

- [ ] **Step 2: Tạo .gitignore và .env.example**

`.gitignore`:
```text
.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
evals/runs/
```

`.env.example`:
```text
OPENROUTER_API_KEY=
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=webscout
```

- [ ] **Step 3: Tạo config.yaml mặc định**

```yaml
researcher:
  model: stealth/ox-alpha
  temperature: 0.2
verifier:
  model: stealth/ox-alpha
  temperature: 0.0
answer:
  model: stealth/ox-alpha
  temperature: 0.3
max_iterations: 3
skills_enabled: false
search:
  max_results: 5
  max_uses: 4
  max_characters: 4000
fetch:
  timeout_seconds: 15.0
  max_chars: 20000
  user_agent: "WebScout/0.1 (research agent)"
```

- [ ] **Step 4: Viết app/config.py**

```python
from functools import lru_cache

from pydantic import AliasChoices, BaseModel, Field
from pydantic_settings import (
    BaseSettings,
    SettingsConfigDict,
    YamlConfigSettingsSource,
)


class RoleConfig(BaseModel):
    model: str = "stealth/ox-alpha"
    temperature: float = 0.0


class SearchConfig(BaseModel):
    max_results: int = 5
    max_uses: int = 4
    max_characters: int = 4000


class FetchConfig(BaseModel):
    timeout_seconds: float = 15.0
    max_chars: int = 20000
    user_agent: str = "WebScout/0.1 (research agent)"


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        yaml_file="config.yaml",
        extra="ignore",
    )

    openrouter_api_key: str = Field(
        default="",
        validation_alias=AliasChoices("OPENROUTER_API_KEY", "WEBCOUT_OPENROUTER_API_KEY"),
    )
    openrouter_base_url: str = "https://openrouter.ai/api/v1"

    researcher: RoleConfig = RoleConfig(temperature=0.2)
    verifier: RoleConfig = RoleConfig(temperature=0.0)
    answer: RoleConfig = RoleConfig(temperature=0.3)

    max_iterations: int = 3
    skills_enabled: bool = False
    search: SearchConfig = SearchConfig()
    fetch: FetchConfig = FetchConfig()

    @classmethod
    def settings_customise_sources(
        cls,
        settings_cls,
        init_settings,
        env_settings,
        dotenv_settings,
        file_secret_settings,
    ):
        return (
            init_settings,
            env_settings,
            dotenv_settings,
            YamlConfigSettingsSource(settings_cls),
            file_secret_settings,
        )


@lru_cache(maxsize=1)
def get_settings() -> Settings:
    return Settings()
```

- [ ] **Step 5: Viết failing test tests/test_config.py**

```python
from pathlib import Path

import pytest

from app.config import Settings


def test_yaml_values_loaded(tmp_path: Path, monkeypatch: pytest.MonkeyPatch):
    monkeypatch.chdir(tmp_path)
    (tmp_path / "config.yaml").write_text(
        "answer:\n  model: other/model\n  temperature: 0.7\nmax_iterations: 2\n",
        encoding="utf-8",
    )
    monkeypatch.setenv("OPENROUTER_API_KEY", "sk-test")
    s = Settings(_env_file=None)  # type: ignore[call-arg]
    assert s.answer.model == "other/model"
    assert s.answer.temperature == 0.7
    assert s.max_iterations == 2
    assert s.openrouter_api_key == "sk-test"


def test_defaults_when_no_files(tmp_path: Path, monkeypatch: pytest.MonkeyPatch):
    monkeypatch.chdir(tmp_path)
    monkeypatch.delenv("OPENROUTER_API_KEY", raising=False)
    s = Settings(_env_file=None)  # type: ignore[call-arg]
    assert s.researcher.model == "stealth/ox-alpha"
    assert s.search == {"max_results": 5, "max_uses": 4, "max_characters": 4000}
```

Lưu ý: `_env_file=None` để test không đọc .env thật từ cwd khác.

- [ ] **Step 6: Cài deps và chạy test**

Run: `uv sync --extra dev; uv run pytest tests/test_config.py -v`
Expected: 2 PASS

- [ ] **Step 7: Tạo bộ khung thư mục rỗng**

Tạo file rỗng `__init__.py`: `app/`, `tests/`, `app/nodes/`, `app/tools/`, `evals/`, `skills/web-research/`.

```powershell
New-Item -ItemType File -Force app/__init__.py, tests/__init__.py, app/nodes/__init__.py, app/tools/__init__.py
```

- [ ] **Step 8: Commit**

```bash
git add pyproject.toml .gitignore .env.example config.yaml app tests uv.lock
git commit -m "chore: scaffold project with settings loading"
```

---

### Task 2: Spike M0 — xác minh R1/R2/R3 (throwaway)

**Files:**
- Create (throwaway, KHÔNG commit): `spike_openrouter.py`
- Create: `docs/superpowers/notes/2026-08-25-m0-spike-findings.md`

**Yêu cầu tiên quyết:** `OPENROUTER_API_KEY` thật trong `.env`. Nếu chưa có key: dừng task này, làm Task 3–5 trước, quay lại trước Task 7.

**Interfaces:**
- Produces: quyết định cơ chế inject server tool (phương án A hay B bên dưới) + vị trí annotations trong AIMessage, ghi trong file findings. Các task sau đọc quyết định này.

- [ ] **Step 1: Viết spike_openrouter.py**

```python
import json
import os

import httpx
from langchain_openai import ChatOpenAI

KEY = os.environ["OPENROUTER_API_KEY"]
BASE = "https://openrouter.ai/api/v1"
MODEL = "stealth/ox-alpha"

SERVER_TOOL = {
    "type": "openrouter:web_search",
    "parameters": {"max_results": 3, "max_uses": 2, "max_characters": 1000},
}


def probe_raw():
    body = {
        "model": MODEL,
        "messages": [{"role": "user", "content": "LangGraph version hien tai la gi? Tra ngan."}],
        "tools": [
            SERVER_TOOL,
            {
                "type": "function",
                "function": {
                    "name": "web_fetch",
                    "description": "Fetch a URL and return readable text.",
                    "parameters": {
                        "type": "object",
                        "properties": {"url": {"type": "string"}},
                        "required": ["url"],
                    },
                },
            },
        ],
    }
    r = httpx.post(
        f"{BASE}/chat/completions",
        headers={"Authorization": f"Bearer {KEY}"},
        json=body,
        timeout=120,
    )
    print("RAW STATUS:", r.status_code)
    data = r.json()
    msg = data["choices"][0]["message"]
    print("RAW KEYS:", sorted(msg.keys()))
    print("ANNOTATIONS:", json.dumps(msg.get("annotations"), ensure_ascii=False)[:2000])
    print("USAGE:", json.dumps(data.get("usage"), ensure_ascii=False))


def probe_langchain_merge():
    m = ChatOpenAI(
        model=MODEL,
        api_key=KEY,
        base_url=BASE,
        extra_body={"tools": [SERVER_TOOL]},
    ).bind_tools(
        [
            {
                "type": "function",
                "function": {
                    "name": "web_fetch",
                    "description": "Fetch a URL.",
                    "parameters": {
                        "type": "object",
                        "properties": {"url": {"type": "string"}},
                        "required": ["url"],
                    },
                },
            }
        ]
    )
    resp = m.invoke("Search web: latest langgraph release. Answer in one line.")
    print("LC TYPE:", type(resp))
    print("LC additional_kwargs keys:", sorted(getattr(resp, "additional_kwargs", {}).keys()))
    print("LC response_metadata:", json.dumps(getattr(resp, "response_metadata", {}), ensure_ascii=False)[:2000])
    print("LC annotations attr:", getattr(resp, "annotations", None))
    print("LC content head:", str(resp.content)[:500])


if __name__ == "__main__":
    probe_raw()
    probe_langchain_merge()
```

Script đọc `OPENROUTER_API_KEY` từ biến môi trường — set trước khi chạy (xem Step 2).

- [ ] **Step 2: Chạy spike**

Run: `$env:OPENROUTER_API_KEY=(Get-Content .env | Select-String '^OPENROUTER_API_KEY=(.+)$').Matches[0].Groups[1].Value; uv run python spike_openrouter.py`
Expected: RAW STATUS 200; thấy `annotations` chứa `url_citation`; USAGE có `server_tool_use.web_search_requests`; phần LC cho biết extra_body.tools có sống sót qua bind_tools hay không (nếu lỗi 4xx/merge thất bại → chọn phương án B).

- [ ] **Step 3: Ghi quyết định vào notes**

Tạo `docs/superpowers/notes/2026-08-25-m0-spike-findings.md` với nội dung bắt buộc:

```markdown
# M0 Spike findings
## R1 - inject server tool
- Phương án A (extra_body={"tools":[spec]} trên ChatOpenAI): <KẾT QUẢ>
- Phương án B (subclass override _get_request_payload): <CHỈ DÙNG KHI A THẤT BẠI>
- Quyết định: <A | B>
## R2 - vị trí url_citation annotations trong AIMessage
- additional_kwargs.annotations: <CÓ/KHÔNG>
- response_metadata: <NỘI DUNG LIÊN QUAN>
- Quyết định extract: <field nào>
## R3 - tool-calling chất lượng ox-alpha
- Model có gọi search đúng lúc không: <CÓ/KHÔNG + ví dụ>
- Go/No-go: <GO | NO-GO + lý do>
```

Phương án B (chỉ code khi A thất bại) — thêm vào `app/models.py` ở Task 6:

```python
class ResearchChatOpenAI(ChatOpenAI):
    server_tools: list = []

    def _get_request_payload(self, *args, **kwargs):
        payload = super()._get_request_payload(*args, **kwargs)
        tools = list(payload.get("tools") or [])
        tools.extend(self.server_tools)
        payload["tools"] = tools
        return payload
```

- [ ] **Step 4: Commit notes (script throwaway không commit)**

```bash
git add docs/superpowers/notes/2026-08-25-m0-spike-findings.md
git commit -m "docs: record m0 spike findings"
Remove-Item spike_openrouter.py
```

---

### Task 3: Schemas + State

**Files:**
- Create: `app/schemas.py`, `app/state.py`
- Test: `tests/test_schemas.py`

**Interfaces:**
- Produces: `Source(TypedDict)`: url,title,source_type,excerpt:str; `Finding(TypedDict)`: claim:str, source_urls:list[str], confidence:str; `VerificationResult(BaseModel)`: sufficient:bool, missing_information:list[str]=[], weak_claims:list[str]=[], contradictory_claims:list[str]=[]; `ResearchState(TypedDict, total=False)` đủ 12 trường theo spec + `search_calls:int`.

- [ ] **Step 1: Viết failing test tests/test_schemas.py**

```python
import pytest
from pydantic import ValidationError

from app.schemas import VerificationResult


def test_verification_result_defaults():
    v = VerificationResult(sufficient=True)
    assert v.missing_information == []
    assert v.weak_claims == []
    assert v.contradictory_claims == []


def test_verification_result_rejects_missing_sufficient():
    with pytest.raises(ValidationError):
        VerificationResult()  # type: ignore[call-arg]


def test_state_has_required_fields():
    from app.state import ResearchState

    hints = ResearchState.__annotations__
    for field in (
        "question",
        "findings",
        "sources",
        "gaps",
        "weak_claims",
        "contradictory_claims",
        "sufficient",
        "iteration",
        "max_iterations",
        "answer",
        "answer_language",
        "search_calls",
    ):
        assert field in hints, field
```

- [ ] **Step 2: Chạy test xác nhận FAIL**

Run: `uv run pytest tests/test_schemas.py -v`
Expected: FAIL — ModuleNotFoundError: app.schemas

- [ ] **Step 3: Viết app/schemas.py**

```python
from typing import TypedDict

from pydantic import BaseModel


class Source(TypedDict):
    url: str
    title: str
    source_type: str
    excerpt: str


class Finding(TypedDict):
    claim: str
    source_urls: list[str]
    confidence: str


class VerificationResult(BaseModel):
    sufficient: bool
    missing_information: list[str] = []
    weak_claims: list[str] = []
    contradictory_claims: list[str] = []
```

- [ ] **Step 4: Viết app/state.py**

```python
from typing import TypedDict


class ResearchState(TypedDict, total=False):
    question: str
    findings: list
    sources: list
    gaps: list
    weak_claims: list
    contradictory_claims: list
    sufficient: bool
    iteration: int
    max_iterations: int
    answer: str
    answer_language: str
    search_calls: int
```

- [ ] **Step 5: Chạy test PASS**

Run: `uv run pytest tests/test_schemas.py -v`
Expected: 3 PASS

- [ ] **Step 6: Commit**

```bash
git add app/schemas.py app/state.py tests/test_schemas.py
git commit -m "feat: add schemas and research state"
```

---

### Task 4: Findings parser + citation reconciliation (pure functions)

**Files:**
- Create: `app/nodes/parsing.py`
- Test: `tests/test_parsing.py`

**Interfaces:**
- Consumes: `Finding`, `Source` từ Task 3.
- Produces:
  - `parse_findings_block(text: str) -> tuple[list[Finding], list[list[str]], str]` — tách khối `## FINDINGS` cuối reply; mỗi dòng dạng `- [S1] claim | confidence: high`; trả `(findings, refs_per_finding, narrative_không_còn_khối)` trong đó `refs_per_finding` là `[["S1"], ["S2"]]`; không có khối → `([], [], text)`.
  - `map_refs_to_urls(findings, refs, citations) -> tuple[list[Finding], list[str]]` — nối mã `[Sn]` → URL theo thứ tự citations (S1 = citation đầu tiên); ref không map được thành chuỗi `unresolved:S<n>`; phần tử thứ hai dành cho mở rộng sau, hiện luôn `[]`.
  - `extract_url_citations(message: Any) -> list[dict]` — quét `message.annotations`, `message.additional_kwargs["annotations"]`, `message.response_metadata["annotations"]`, trả list dict `url_citation` có `url`.
  - `reconcile_sources(findings, citations) -> tuple[list[Source], list[str]]` — dedupe citations thành Source (source_type="secondary", excerpt cắt 500 ký tự); trả `(sources, unknown_urls)` với unknown = URL/ref `unresolved:` trong findings không có trong citations.

- [ ] **Step 1: Viết failing test tests/test_parsing.py**

```python
from types import SimpleNamespace

from app.nodes.parsing import (
    extract_url_citations,
    parse_findings_block,
    reconcile_sources,
)

SAMPLE = """Nghiên cứu tổng hợp.

LangGraph chạy vòng lặp state machine còn Temporal chạy workflow durable.

## FINDINGS
- [S1] LangGraph is a state-machine orchestration library | confidence: high
- [S2] Temporal provides durable execution with automatic replay | confidence: medium
"""


def test_parse_findings_block_extracts_and_strips():
    findings, narrative = parse_findings_block(SAMPLE)
    assert len(findings) == 2
    assert findings[0]["claim"].startswith("LangGraph is")
    assert findings[0]["source_refs"] == ["S1"]
    assert findings[0]["confidence"] == "high"
    assert "FINDINGS" not in narrative
    assert narrative.strip().startswith("Nghiên cứu")


def test_parse_findings_block_absent():
    findings, narrative = parse_findings_block("Chỉ là văn bản thường.")
    assert findings == []
    assert narrative == "Chỉ là văn bản thường."


def test_parse_findings_bad_confidence_skipped():
    text = "## FINDINGS\n- [S1] claim one | confidence: very-high\n"
    findings, _ = parse_findings_block(text)
    assert findings == []


def _msg(annotations):
    return SimpleNamespace(
        annotations=annotations,
        additional_kwargs={},
        response_metadata={},
    )


def test_extract_url_citations_from_annotations_attr():
    m = _msg(
        [
            {
                "type": "url_citation",
                "url_citation": {"url": "https://a.dev", "title": "A", "content": "ca"},
            },
            {"type": "other"},
        ]
    )
    cites = extract_url_citations(m)
    assert cites == [{"url": "https://a.dev", "title": "A", "content": "ca"}]


def test_extract_url_citations_from_additional_kwargs():
    m = SimpleNamespace(
        annotations=None,
        additional_kwargs={
            "annotations": [
                {"url_citation": {"url": "https://b.dev", "title": "B", "content": ""}}
            ]
        },
        response_metadata={},
    )
    assert len(extract_url_citations(m)) == 1


def test_reconcile_sources_dedupes_and_flags_unknown():
    findings = [
        {"claim": "x", "source_urls": ["https://a.dev"], "confidence": "high"},
        {"claim": "y", "source_urls": ["https://ghost.dev"], "confidence": "low"},
    ]
    citations = [
        {"url": "https://a.dev", "title": "A", "content": "long" * 300},
        {"url": "https://a.dev", "title": "A dup", "content": ""},
    ]
    sources, unknown = reconcile_sources(findings, citations)
    assert len(sources) == 1
    assert sources[0]["url"] == "https://a.dev"
    assert sources[0]["source_type"] == "secondary"
    assert len(sources[0]["excerpt"]) <= 500
    assert unknown == ["https://ghost.dev"]


def test_parse_maps_source_refs_to_urls():
    findings, refs, _ = parse_findings_block(SAMPLE)
    citations = [
        {"url": "https://langchain.ai", "title": "LG docs", "content": ""},
        {"url": "https://temporal.io", "title": "Temporal docs", "content": ""},
    ]
    mapped, unknown = map_refs_to_urls(findings, refs, citations)
    assert mapped[0]["source_urls"] == ["https://langchain.ai"]
    assert mapped[1]["source_urls"] == ["https://temporal.io"]
    assert unknown == []


def test_map_refs_unresolved_marker():
    findings, refs, _ = parse_findings_block(
        "## FINDINGS\n- [S9] Ghost | confidence: low\n"
    )
    mapped, _ = map_refs_to_urls(
        findings, refs, [{"url": "https://y.dev", "title": "Y", "content": ""}]
    )
    assert mapped[0]["source_urls"] == ["unresolved:S9"]


def test_reconcile_flags_unresolved_prefix():
    findings = [
        {"claim": "g", "source_urls": ["unresolved:S9"], "confidence": "low"}
    ]
    sources, unknown = reconcile_sources(findings, [])
    assert unknown == ["unresolved:S9"]
    assert sources == []
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_parsing.py -v`
Expected: FAIL — No module named app.nodes.parsing

- [ ] **Step 3: Viết app/nodes/parsing.py**

```python
import re
from typing import Any

from app.schemas import Finding, Source

_LINE_RE = re.compile(
    r"^-\s*\[(?P<ref>S\d+)\]\s+(?P<claim>.+?)\s*\|\s*confidence:\s*"
    r"(?P<conf>high|medium|low)\s*$",
    re.IGNORECASE,
)
_BLOCK_RE = re.compile(r"^##\s*FINDINGS\s*$", re.IGNORECASE | re.MULTILINE)


def parse_findings_block(text: str) -> tuple[list[Finding], list[list[str]], str]:
    match = _BLOCK_RE.search(text)
    if not match:
        return [], [], text
    narrative = text[: match.start()].rstrip()
    findings: list[Finding] = []
    refs: list[list[str]] = []
    for raw in text[match.end() :].splitlines():
        line = raw.strip()
        if not line:
            continue
        m = _LINE_RE.match(line)
        if not m:
            continue
        findings.append(
            Finding(
                claim=m.group("claim").strip(),
                source_urls=[],
                confidence=m.group("conf").lower(),
            )
        )
        refs.append([m.group("ref")])
    return findings, refs, narrative


def map_refs_to_urls(
    findings: list[Finding], refs: list[list[str]], citations: list[dict]
) -> tuple[list[Finding], list[str]]:
    ordered_urls = [c.get("url") for c in citations if c.get("url")]
    ref_to_url = {f"S{i + 1}": u for i, u in enumerate(ordered_urls)}
    mapped: list[Finding] = []
    for finding, finding_refs in zip(findings, refs):
        urls: list[str] = []
        for r in finding_refs:
            if r in ref_to_url:
                urls.append(ref_to_url[r])
            else:
                urls.append(f"unresolved:{r}")
        mapped.append(Finding(
            claim=finding["claim"],
            source_urls=list(dict.fromkeys(urls)),
            confidence=finding["confidence"],
        ))
    return mapped, []


def extract_url_citations(message: Any) -> list[dict]:
    containers = [
        getattr(message, "annotations", None),
        (getattr(message, "additional_kwargs", None) or {}).get("annotations"),
        (getattr(message, "response_metadata", None) or {}).get("annotations"),
    ]
    out: list[dict] = []
    for anns in containers:
        if not anns:
            continue
        if isinstance(anns, dict):
            anns = [anns]
        for entry in anns:
            if not isinstance(entry, dict):
                continue
            cite = entry.get("url_citation") or (
                entry if entry.get("url") else None
            )
            if isinstance(cite, dict) and cite.get("url"):
                out.append(cite)
    seen: set[str] = set()
    deduped = [c for c in out if not (c["url"] in seen or seen.add(c["url"]))]
    return deduped


def reconcile_sources(
    findings: list[Finding], citations: list[dict]
) -> tuple[list[Source], list[str]]:
    sources: list[Source] = []
    known: set[str] = set()
    for cite in citations:
        url = cite.get("url")
        if not url or url in known:
            continue
        known.add(url)
        sources.append(
            Source(
                url=url,
                title=cite.get("title") or url,
                source_type="secondary",
                excerpt=(cite.get("content") or "")[:500],
            )
        )
    unknown: list[str] = []
    for finding in findings:
        for url in finding.get("source_urls", []):
            resolvable = url.startswith("http") or url.startswith("unresolved:")
            if url not in known and url not in unknown and resolvable:
                unknown.append(url)
    return sources, unknown
```

Ghi chú thiết kế: `parse_findings_block` trả thêm `refs` ([["S1"], ...]) vì dòng FINDINGS chỉ ghi mã [Sn]; `map_refs_to_urls` nối mã → URL theo thứ tự citations (S1 = citation đầu tiên); mã không khớp trở thành chuỗi `unresolved:S<n>` để `reconcile_sources` bắt vào unknown — đây là hợp đồng với system prompt ở Task 8: agent PHẢI liệt kê citations theo đúng thứ tự nó tham chiếu.

- [ ] **Step 4: Chạy PASS**

Run: `uv run pytest tests/test_parsing.py -v`
Expected: 9 PASS

- [ ] **Step 5: Commit**

```bash
git add app/nodes/parsing.py tests/test_parsing.py
git commit -m "feat: findings parser and citation reconciliation"
```

---

### Task 5: Model factory

**Files:**
- Create: `app/models.py`
- Test: `tests/test_models.py`

**Interfaces:**
- Consumes: `get_settings()`, `RoleConfig` (Task 1).
- Produces: `get_model(role: str = "researcher", settings: Settings | None = None) -> ChatOpenAI` — model=`<role>.model`, temperature=`<role>.temperature`, api_key=settings.openrouter_api_key, base_url=settings.openrouter_base_url. Nếu spike chọn phương án B: class `ResearchChatOpenAI(ChatOpenAI)` với field `server_tools: list` như code trong Task 2 Step 3, và `get_model("researcher")` trả instance đó.

- [ ] **Step 1: Viết failing test tests/test_models.py**

```python
from app.config import RoleConfig, Settings
from app.models import get_model


def _settings() -> Settings:
    return Settings(_env_file=None)  # type: ignore[call-arg]


def test_get_model_verifier_config():
    s = _settings()
    m = get_model("verifier", s)
    assert m.model_name == s.verifier.model
    assert m.temperature == s.verifier.temperature
    assert m.openai_api_base == "https://openrouter.ai/api/v1"


def test_get_model_custom_role_values():
    s = _settings()
    s.researcher = RoleConfig(model="some/model", temperature=0.55)
    m = get_model("researcher", s)
    assert m.model_name == "some/model"
    assert abs(m.temperature - 0.55) < 1e-9


def test_invalid_role_raises():
    import pytest

    with pytest.raises(ValueError):
        get_model("nonexistent", _settings())
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_models.py -v`
Expected: FAIL — No module named app.models

- [ ] **Step 3: Viết app/models.py**

```python
from langchain_openai import ChatOpenAI

from app.config import RoleConfig, get_settings

ROLES = ("researcher", "verifier", "answer")


def get_model(role: str = "researcher", settings=None):
    if role not in ROLES:
        raise ValueError(f"unknown role: {role}")
    s = settings or get_settings()
    cfg: RoleConfig = getattr(s, role)
    return ChatOpenAI(
        model=cfg.model,
        temperature=cfg.temperature,
        api_key=s.openrouter_api_key or "not-set",
        base_url=s.openrouter_base_url,
    )
```

Nếu notes spike (Task 2) kết luận phương án B: thêm subclass `ResearchChatOpenAI` (code ở Task 2 Step 3, field `server_tools`) và trong `get_model` trả instance subclass khi `role == "researcher"`.

- [ ] **Step 4: Chạy PASS**

Run: `uv run pytest tests/test_models.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add app/models.py tests/test_models.py
git commit -m "feat: openrouter model factory"
```

---

### Task 6: web_fetch tool

**Files:**
- Create: `app/tools/fetch.py`
- Test: `tests/test_fetch.py`, fixture `tests/fixtures/page.html`

**Interfaces:**
- Consumes: `FetchConfig` (Task 1).
- Produces: `make_web_fetch(cfg: FetchConfig)` trả về LangChain tool `web_fetch(url: str) -> str` (sync); helper thuần `clean_html(html: str, max_chars: int) -> str`.

- [ ] **Step 1: Tạo fixture tests/fixtures/page.html**

```html
<!doctype html>
<html><head><title>Fixture</title>
<style>.x{color:red}</style></head>
<body>
<nav>menu bar links</nav>
<article>
<h1>Deep Agents</h1>
<p>Deep Agents is an agent harness built on LangGraph with skills and subagents.</p>
<p>It supports any tool-calling model through LangChain chat models.</p>
</article>
<script>window.tracker=1;</script>
<footer>copyright footer</footer>
</body></html>
```

- [ ] **Step 2: Viết failing test tests/test_fetch.py**

```python
from pathlib import Path

from app.config import FetchConfig
from app.tools.fetch import clean_html, make_web_fetch

FIXTURE = Path(__file__).parent / "fixtures" / "page.html"


def test_clean_html_extracts_article_text():
    html = FIXTURE.read_text(encoding="utf-8")
    text = clean_html(html, max_chars=5000)
    assert "agent harness built on LangGraph" in text
    assert "window.tracker" not in text
    assert ".x{color:red}" not in text


def test_clean_html_truncates():
    html = "<p>" + ("word " * 5000) + "</p>"
    assert len(clean_html(html, max_chars=200)) <= 210


def test_tool_returns_error_for_bad_scheme():
    tool = make_web_fetch(FetchConfig())
    out = tool.invoke({"url": "ftp://example.com/file"})
    assert out.startswith("FETCH_ERROR")


def test_tool_returns_error_for_unreachable_host():
    tool = make_web_fetch(FetchConfig(timeout_seconds=2.0))
    out = tool.invoke({"url": "http://127.0.0.1:9/nope"})
    assert out.startswith("FETCH_ERROR")
```

- [ ] **Step 3: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_fetch.py -v`
Expected: FAIL — No module named app.tools.fetch

- [ ] **Step 4: Viết app/tools/fetch.py**

```python
import re

import httpx
import trafilatura
from langchain_core.tools import tool

from app.config import FetchConfig


def clean_html(html: str, max_chars: int) -> str:
    extracted = trafilatura.extract(
        html, output_format="txt", include_links=False, favor_recall=True
    )
    if not extracted:
        stripped = re.sub(r"<(script|style)[\s\S]*?</\1>", " ", html, flags=re.I)
        stripped = re.sub(r"<[^>]+>", " ", stripped)
        extracted = re.sub(r"\s+", " ", stripped).strip()
    return extracted[:max_chars]


def make_web_fetch(cfg: FetchConfig):
    @tool
    def web_fetch(url: str) -> str:
        """Fetch a web page and return its readable main text."""
        try:
            resp = httpx.get(
                url,
                timeout=cfg.timeout_seconds,
                headers={"User-Agent": cfg.user_agent},
                follow_redirects=True,
            )
            resp.raise_for_status()
        except Exception as exc:
            return f"FETCH_ERROR: {type(exc).__name__}: {exc}"
        ctype = resp.headers.get("content-type", "")
        if "html" not in ctype and "text" not in ctype:
            return f"FETCH_ERROR: unsupported content-type {ctype}"
        return clean_html(resp.text, cfg.max_chars)

    return web_fetch
```

- [ ] **Step 5: Chạy PASS**

Run: `uv run pytest tests/test_fetch.py -v`
Expected: 4 PASS

- [ ] **Step 6: Commit**

```bash
git add app/tools/fetch.py tests/test_fetch.py tests/fixtures/page.html
git commit -m "feat: web_fetch tool via httpx and trafilatura"
```

---

### Task 7: Search server-tool adapter

**Files:**
- Create: `app/tools/search.py`
- Test: `tests/test_search_adapter.py`

**Interfaces:**
- Consumes: `SearchConfig` (Task 1); quyết định phương án A/B từ notes spike (Task 2).
- Produces:
  - `build_search_spec(search: SearchConfig) -> dict` — đúng shape server tool.
  - `count_web_searches(result: Any) -> int` — đọc `usage.server_tool_use.web_search_requests` từ dict usage hoặc message metadata; thiếu → 0.
  - `attach_server_tools(model, specs: list[dict])` — phương án A: `model.bind(extra_body={"tools": specs})`; phương án B: gán `model.server_tools = specs` (model là ResearchChatOpenAI). Task 8 dùng hàm này duy nhất, nên đổi phương án chỉ sửa đây.

- [ ] **Step 1: Viết failing test tests/test_search_adapter.py**

```python
from types import SimpleNamespace

from app.config import SearchConfig
from app.tools.search import build_search_spec, count_web_searches


def test_build_search_spec_shape():
    spec = build_search_spec(SearchConfig())
    assert spec["type"] == "openrouter:web_search"
    p = spec["parameters"]
    assert p == {"max_results": 5, "max_uses": 4, "max_characters": 4000}


def test_count_from_usage_dict():
    assert count_web_searches({"usage": {"server_tool_use": {"web_search_requests": 3}}}) == 3


def test_count_from_message_response_metadata():
    msg = SimpleNamespace(
        response_metadata={"usage": {"server_tool_use": {"web_search_requests": 2}}},
        additional_kwargs={},
    )
    assert count_web_searches(msg) == 2


def test_count_missing_is_zero():
    assert count_web_searches({"usage": {}}) == 0
    assert count_web_searches(None) == 0
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_search_adapter.py -v`
Expected: FAIL — No module named app.tools.search

- [ ] **Step 3: Viết app/tools/search.py**

```python
from typing import Any

from app.config import SearchConfig


def build_search_spec(search: SearchConfig) -> dict:
    return {
        "type": "openrouter:web_search",
        "parameters": {
            "max_results": search.max_results,
            "max_uses": search.max_uses,
            "max_characters": search.max_characters,
        },
    }


def _usage_of(result: Any) -> dict:
    if result is None:
        return {}
    if isinstance(result, dict):
        return result.get("usage") or {}
    meta = getattr(result, "response_metadata", None) or {}
    return meta.get("usage") or {}


def count_web_searches(result: Any) -> int:
    usage = _usage_of(result)
    stu = usage.get("server_tool_use") if isinstance(usage, dict) else None
    value = stu.get("web_search_requests", 0) if isinstance(stu, dict) else 0
    try:
        return int(value)
    except (TypeError, ValueError):
        return 0


def attach_server_tools(model, specs: list[dict]):
    return model.bind(extra_body={"tools": specs})
```

Nếu notes spike kết luận phương án B: thay thân `attach_server_tools` bằng `model.server_tools = list(specs); return model` và đảm bảo Task 5 tạo researcher bằng `ResearchChatOpenAI`.

- [ ] **Step 4: Chạy PASS**

Run: `uv run pytest tests/test_search_adapter.py -v`
Expected: 4 PASS

- [ ] **Step 5: Commit**

```bash
git add app/tools/search.py tests/test_search_adapter.py
git commit -m "feat: openrouter web_search server tool adapter"
```

---

### Task 8: Deep Agent assembly

**Files:**
- Create: `app/agent.py`
- Test: `tests/test_agent.py`

**Interfaces:**
- Consumes: `get_model` (Task 5), `make_web_fetch` (Task 6), `build_search_spec` + `attach_server_tools` (Task 7), `Settings.skills_enabled`.
- Produces: `RESEARCH_SYSTEM_PROMPT: str`; `build_research_agent(settings=None)` → compiled graph của deepagents, có tool `web_fetch`, server tool search, system prompt ép format FINDINGS; khi `skills_enabled=True` và tồn tại `skills/` thì truyền `backend=FilesystemBackend(root_dir=".")`, `skills=["skills/"]`.

- [ ] **Step 1: Viết failing test tests/test_agent.py**

```python
from app.agent import RESEARCH_SYSTEM_PROMPT, build_research_agent
from app.config import Settings


def test_system_prompt_contains_contract():
    p = RESEARCH_SYSTEM_PROMPT
    assert "## FINDINGS" in p
    assert "[S1]" in p
    assert "confidence" in p
    assert "Never invent" in p


def test_build_research_agent_offline_construction(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    s = Settings(_env_file=None)  # type: ignore[call-arg]
    s.skills_enabled = False
    agent = build_research_agent(s)
    assert agent is not None
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_agent.py -v`
Expected: FAIL — No module named app.agent

- [ ] **Step 3: Viết app/agent.py**

```python
from pathlib import Path

from app.config import Settings, get_settings
from app.models import get_model
from app.tools.fetch import make_web_fetch
from app.tools.search import attach_server_tools, build_search_spec

RESEARCH_SYSTEM_PROMPT = """You are WebScout's research agent.

Mission: answer the research request using current web evidence.

Method:
1. Break the request into factual claims that need evidence.
2. Use the provided web search capability to find candidate sources.
3. Use the web_fetch tool to read promising pages. Prefer primary sources:
   official documentation, standards, government sites, academic papers,
   primary company sources. Use secondary sources only for interpretation.
4. Cross-check contested claims across at least two sources when possible.
5. Track uncertainty. If credible sources disagree, say so explicitly.

Integrity rules:
- Never invent a URL, citation or source. Reference ONLY URLs that appeared
  in your search results or that you fetched yourself.
- For each numbered citation you produce, the corresponding URL must be
  listed in SOURCES YOU USED below, in exactly that order.

Output contract — end EVERY final reply with exactly this block:

## FINDINGS
- [S1] <one factual claim> | confidence: high|medium|low
- [S2] <one factual claim> | confidence: high|medium|low

Where [Sn] refers to the nth URL in the sources you used, counted in the
order you first used them. One line per claim. No prose inside the block."""


def build_research_agent(settings: Settings | None = None):
    s = settings or get_settings()
    base_model = get_model("researcher", s)
    model = attach_server_tools(base_model, [build_search_spec(s.search)])
    kwargs = dict(
        model=model,
        tools=[make_web_fetch(s.fetch)],
        system_prompt=RESEARCH_SYSTEM_PROMPT,
    )
    if s.skills_enabled and Path("skills").is_dir():
        from deepagents.backends.filesystem import FilesystemBackend

        kwargs["backend"] = FilesystemBackend(root_dir=".")
        kwargs["skills"] = ["skills/"]
    from deepagents import create_deep_agent

    return create_deep_agent(**kwargs)
```

- [ ] **Step 4: Chạy PASS**

Run: `uv run pytest tests/test_agent.py -v`
Expected: 2 PASS

- [ ] **Step 5: Commit**

```bash
git add app/agent.py tests/test_agent.py
git commit -m "feat: assemble research deep agent"
```

---

### Task 9: CLI one-shot (V0)

**Files:**
- Create: `app/main.py`
- Test: `tests/test_cli.py`

**Interfaces:**
- Consumes: `build_research_agent` (Task 8), `parse_findings_block`, `map_refs_to_urls`, `extract_url_citations` (Task 4), `count_web_searches` (Task 7), `get_settings`.
- Produces: `run_question(question: str, agent=None) -> dict` trả `{"answer": str, "sources": list[Source], "findings": list, "search_calls": int}`; `main(argv=None)` CLI: `webscout "câu hỏi"` one-shot; không đối số → chế độ tương tác.

- [ ] **Step 1: Viết failing test tests/test_cli.py**

```python
import pytest

from app.main import run_question


class FakeAgent:
    def invoke(self, payload, config=None):
        from langchain_core.messages import AIMessage

        return {
            "messages": [
                AIMessage(
                    content=(
                        "Answer body citing [S1].\n\n## FINDINGS\n"
                        "- [S1] LG runs on LangGraph | confidence: high\n"
                    ),
                    additional_kwargs={
                        "annotations": [
                            {
                                "url_citation": {
                                    "url": "https://docs.langchain.com/lg",
                                    "title": "LG Docs",
                                    "content": "lg docs text",
                                }
                            }
                        ]
                    },
                )
            ]
        }


def test_run_question_assembles_result():
    out = run_question("So sánh?", agent=FakeAgent())
    assert "[S1]" in out["answer"]
    assert out["sources"][0]["url"] == "https://docs.langchain.com/lg"
    assert out["sources"][0]["source_type"] == "secondary"
    assert out["findings"][0]["claim"].startswith("LG runs")


@pytest.mark.integration
def test_cli_real_roundtrip():
    import os

    if not os.environ.get("OPENROUTER_API_KEY"):
        pytest.skip("needs OPENROUTER_API_KEY")
    out = run_question("Deep agents la gi? Tra loii ngan.")
    assert out["sources"], "expected at least one citation"
    assert "[S" in out["answer"] or out["answer"].strip()
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_cli.py -v -m "not integration"`
Expected: FAIL — No module named app.main

- [ ] **Step 3: Viết app/main.py**

```python
import argparse
import sys

from app.agent import build_research_agent
from app.config import get_settings
from app.nodes.parsing import (
    extract_url_citations,
    map_refs_to_urls,
    parse_findings_block,
    reconcile_sources,
)
from app.tools.search import count_web_searches


def run_question(question: str, agent=None, settings=None) -> dict:
    s = settings or get_settings()
    deep = agent or build_research_agent(s)
    result = deep.invoke(
        {"messages": [{"role": "user", "content": question}]},
        config={"recursion_limit": 50},
    )
    message = result["messages"][-1]
    findings, refs, narrative = parse_findings_block(str(message.content))
    citations = extract_url_citations(message)
    findings, _ = map_refs_to_urls(findings, refs, citations)
    sources, unknown = reconcile_sources(findings, citations)
    searches = count_web_searches(message)
    if unknown:
        print(f"[warn] {len(unknown)} uncited URL(s) ignored", file=sys.stderr)
    return {
        "answer": narrative.strip(),
        "sources": sources,
        "findings": findings,
        "search_calls": searches,
    }


def _print_result(out: dict) -> None:
    print("\n=== ANSWER ===\n")
    print(out["answer"])
    if out["sources"]:
        print("\n=== SOURCES ===")
        for i, src in enumerate(out["sources"], 1):
            print(f"[{i}] {src['title']} — {src['url']}")


def main(argv=None) -> None:
    parser = argparse.ArgumentParser(prog="webscout")
    parser.add_argument("question", nargs="*", help="research question")
    args = parser.parse_args(argv)
    questions = [" ".join(args.question)] if args.question else None
    while True:
        q = questions.pop(0) if questions else input("\nwebscout> ").strip()
        if not q or q.lower() in {"exit", "quit"}:
            break
        _print_result(run_question(q))
        if questions:
            break


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Chạy PASS**

Run: `uv run pytest tests/test_cli.py -v -m "not integration"`
Expected: 1 PASS (integration skip/fail chỉ khi có key)

- [ ] **Step 5: Smoke thử nghiệm thật (nếu đã có key)**

Run: `$env:OPENROUTER_API_KEY="<key>"; uv run webscout "Deep Agents khác LangChain create_agent ở điểm nào? Trả lời ngắn."`
Expected: in ANSWER + SOURCES; có ≥1 citation.

- [ ] **Step 6: Commit**

```bash
git add app/main.py tests/test_cli.py
git commit -m "feat: v0 cli one-shot research"
```

---

### Task 10: Acceptance V0

**Không có file mới.** Checklist chạy tay, ghi kết quả vào `docs/superpowers/notes/v0-acceptance.md`.

- [ ] **Step 1: Chạy integration test**

Run: `$env:OPENROUTER_API_KEY="<key>"; uv run pytest -m integration -v`
Expected: test_cli_real_roundtrip PASS

- [ ] **Step 2: Kiểm tra LangSmith**

Mở project `webscout` trên LangSmith UI. Expected: run mới xuất hiện, cây trace có các bước model/tool bên trong deep agent.

- [ ] **Step 3: Ghi acceptance + commit**

`v0-acceptance.md` ghi: câu hỏi thử, số sources, số lần search (stderr/stdout log), link trace LangSmith.

```bash
git add docs/superpowers/notes/v0-acceptance.md
git commit -m "docs: v0 acceptance notes"
```

---

### Task 11: Research node (graph-shaped)

**Files:**
- Modify: `app/nodes/research.py` (tạo mới; parsing giữ nguyên ở parsing.py)
- Test: `tests/test_node_research.py`

**Interfaces:**
- Consumes: `build_research_agent`, parsing helpers, `count_web_searches`, `Settings`.
- Produces: `make_research_node(agent, settings) -> Callable[[ResearchState], dict]`; node nhận state, trả delta: `findings, sources, gaps(không đổi), weak_claims(danh sách URL lạ dạng chuỗi mô tả), iteration(+1), search_calls(cộng dồn)`. Helper thuần `build_research_input(state) -> str`.

- [ ] **Step 1: Viết failing test tests/test_node_research.py**

```python
from types import SimpleNamespace

from app.config import Settings
from app.nodes.research import build_research_input, make_research_node


class FakeAgent:
    def __init__(self, msg):
        self._msg = msg

    def invoke(self, payload, config=None):
        self.last_payload = payload
        return {"messages": [self._msg]}


def _msg(content, citations):
    return SimpleNamespace(
        content=content,
        annotations=[{"url_citation": c} for c in citations],
        additional_kwargs={},
        response_metadata={"usage": {"server_tool_use": {"web_search_requests": 2}}},
    )


CONTENT = (
    "Body.\n\n## FINDINGS\n- [S1] Claim A | confidence: high\n"
)


def test_first_iteration_prompt_only_question():
    s = Settings(_env_file=None)  # type: ignore[call-arg]
    text = build_research_input({"question": "Q?", "iteration": 0})
    assert "Q?" in text
    assert "Existing findings" not in text


def test_followup_prompt_includes_gaps():
    s = Settings(_env_file=None)  # type: ignore[call-arg]
    text = build_research_input(
        {
            "question": "Q?",
            "iteration": 1,
            "findings": [],
            "gaps": ["replay semantics"],
        }
    )
    assert "replay semantics" in text
    assert "ONLY these points" in text


def test_node_updates_state():
    s = Settings(_env_file=None)  # type: ignore[call-arg]
    agent = FakeAgent(
        _msg(CONTENT, [{"url": "https://x.dev", "title": "X", "content": "cx"}])
    )
    node = make_research_node(agent, s)
    delta = node(
        {"question": "Q?", "iteration": 0, "gaps": [], "weak_claims": []}
    )
    assert delta["iteration"] == 1
    assert delta["search_calls"] == 2
    assert delta["sources"][0]["url"] == "https://x.dev"
    assert delta["findings"][0]["source_urls"] == ["https://x.dev"]


def test_unknown_ref_becomes_weak_claim():
    s = Settings(_env_file=None)  # type: ignore[call-arg]
    bad = "Body.\n\n## FINDINGS\n- [S9] Ghost claim | confidence: low\n"
    agent = FakeAgent(_msg(bad, [{"url": "https://y.dev", "title": "Y", "content": ""}]))
    delta = make_research_node(agent, s)(
        {"question": "Q?", "iteration": 1, "gaps": [], "weak_claims": []}
    )
    assert any("Ghost claim" in w for w in delta["weak_claims"])
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_node_research.py -v`
Expected: FAIL — No module named app.nodes.research

- [ ] **Step 3: Viết app/nodes/research.py**

```python
import json
from typing import Callable

from app.config import Settings
from app.nodes.parsing import (
    extract_url_citations,
    map_refs_to_urls,
    parse_findings_block,
    reconcile_sources,
)
from app.state import ResearchState
from app.tools.search import count_web_searches


def build_research_input(state: ResearchState) -> str:
    parts = [f"Original question:\n{state['question']}"]
    if state.get("iteration", 0) > 0:
        existing = state.get("findings") or []
        if existing:
            parts.append(
                "Existing findings:\n"
                + json.dumps(existing, ensure_ascii=False, indent=2)
            )
        gaps = state.get("gaps") or []
        if gaps:
            parts.append(
                "Missing evidence — research ONLY these points:\n- "
                + "\n- ".join(gaps)
            )
    return "\n\n".join(parts)


def make_research_node(agent, settings: Settings) -> Callable[[ResearchState], dict]:
    def research(state: ResearchState) -> dict:
        prompt = build_research_input(state)
        result = agent.invoke(
            {"messages": [{"role": "user", "content": prompt}]},
            config={"recursion_limit": 50},
        )
        message = result["messages"][-1]
        findings, refs, _ = parse_findings_block(str(message.content))
        citations = extract_url_citations(message)
        findings, _ = map_refs_to_urls(findings, refs, citations)
        sources, unknown = reconcile_sources(findings, citations)
        unknown_set = set(unknown)
        seen_weak: set[str] = set()
        weak: list[str] = []
        for finding in findings:
            for u in finding.get("source_urls", []):
                if u in unknown_set and u.startswith("unresolved:"):
                    item = (
                        f"claim '{finding.get('claim', '')}' references "
                        f"unknown citation: {u.replace('unresolved:', '[ref] ')}"
                    )
                    if item not in seen_weak:
                        seen_weak.add(item)
                        weak.append(item)
        prior_weak = state.get("weak_claims") or []
        return {
            "findings": findings,
            "sources": sources,
            "weak_claims": prior_weak + weak,
            "iteration": state.get("iteration", 0) + 1,
            "search_calls": state.get("search_calls", 0)
            + count_web_searches(message),
        }

    return research
```

Hành vi `unresolved:S<n>` (ref không khớp citation nào) đã có sẵn từ Task 4: `reconcile_sources` trả chúng trong `unknown`, node chuyển thành `weak_claims` dạng "finding references unknown citation: [ref] S9" — không URL nào bị drop im lặng.

- [ ] **Step 4: Chạy toàn bộ suite**

Run: `uv run pytest -m "not integration" -v`
Expected: tất cả PASS

- [ ] **Step 5: Commit**

```bash
git add app/nodes/research.py tests/test_node_research.py
git commit -m "feat: graph research node with gap-driven re-query"
```

---

### Task 12: Verify node

**Files:**
- Create: `app/nodes/verify.py`
- Test: `tests/test_node_verify.py`

**Interfaces:**
- Consumes: `get_model("verifier")`, `VerificationResult` (Task 3).
- Produces: `VERIFY_SYSTEM_PROMPT: str`; `make_verify_node(settings) -> Callable[[ResearchState], dict]` trả delta `{"sufficient", "gaps", "weak_claims"(gộp unique), "contradictory_claims"}`. Cho phép inject model: `make_verify_node(settings, model=None)`.

- [ ] **Step 1: Viết failing test tests/test_node_verify.py**

```python
from langchain_core.language_models.fake_chat_models import (
    GenericFakeChatModel,
)
from langchain_core.messages import AIMessage

from app.config import Settings
from app.nodes.verify import make_verify_node


def _fake_model(payload_json: str):
    return GenericFakeChatModel(messages=iter([AIMessage(content=payload_json)]))


STATE = {
    "question": "Q?",
    "findings": [{"claim": "A", "source_urls": ["https://a"], "confidence": "high"}],
    "sources": [{"url": "https://a", "title": "A", "source_type": "secondary", "excerpt": ""}],
    "iteration": 1,
    "max_iterations": 3,
    "weak_claims": ["old weak"],
}


def test_verify_merges_result():
    payload = (
        '{"sufficient": false, "missing_information": ["replay"], '
        '"weak_claims": ["old weak", "new weak"], "contradictory_claims": []}'
    )
    node = make_verify_node(Settings(_env_file=None), model=_fake_model(payload))  # type: ignore[call-arg]
    delta = node(dict(STATE))
    assert delta["sufficient"] is False
    assert delta["gaps"] == ["replay"]
    assert delta["weak_claims"] == ["old weak", "new weak"]


def test_verify_sufficient_clears_gaps():
    payload = '{"sufficient": true, "missing_information": [], "weak_claims": [], "contradictory_claims": []}'
    node = make_verify_node(Settings(_env_file=None), model=_fake_model(payload))  # type: ignore[call-arg]
    delta = node(dict(STATE))
    assert delta["sufficient"] is True
    assert delta["gaps"] == []
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_node_verify.py -v`
Expected: FAIL — No module named app.nodes.verify

- [ ] **Step 3: Viết app/nodes/verify.py**

Quyết định thiết kế: KHÔNG dùng `with_structured_output` của langchain (ràng buộc tool-call, khó fake offline và lệch hành vi giữa các model OpenRouter). Thay bằng: yêu cầu JSON trong prompt + parse thủ công + Pydantic `VerificationResult` validate — vẫn đảm bảo schema, deterministic với mọi model.

```python
import json
import re
from typing import Callable

from pydantic import ValidationError

from app.config import Settings
from app.schemas import VerificationResult
from app.state import ResearchState

VERIFY_SYSTEM_PROMPT = """You are an independent evidence auditor.

Given a research question, findings with cited sources, decide whether the
evidence is sufficient to write a correct, well-cited answer.

Checklist:
- Does the evidence cover every part of the question?
- Are important claims backed by sources?
- Are there contradictions between credible sources?
- Are claims relying on weak/secondary-only evidence flagged?

Be strict but pragmatic: minor stylistic gaps are fine to pass.

Reply with JSON only, exactly this shape:
{"sufficient": bool, "missing_information": [str], "weak_claims": [str], "contradictory_claims": [str]}"""


def _parse_verdict(raw: str) -> VerificationResult:
    match = re.search(r"\{[\s\S]*\}", raw)
    if not match:
        raise ValueError(f"no JSON object in verifier reply: {raw[:200]}")
    try:
        return VerificationResult.model_validate_json(match.group(0))
    except ValidationError as exc:
        raise ValueError(f"verifier JSON invalid: {exc}") from exc


def make_verify_node(settings: Settings, model=None) -> Callable[[ResearchState], dict]:
    from app.models import get_model

    llm = model or get_model("verifier", settings)

    def verify(state: ResearchState) -> dict:
        human = (
            f"Question:\n{state['question']}\n\n"
            "Findings:\n"
            + json.dumps(state.get("findings") or [], ensure_ascii=False, indent=2)
            + "\n\nSources:\n"
            + json.dumps(state.get("sources") or [], ensure_ascii=False, indent=2)
        )
        reply = llm.invoke(
            [("system", VERIFY_SYSTEM_PROMPT), ("human", human)]
        )
        result = _parse_verdict(str(reply.content))
        merged_weak = list(dict.fromkeys(
            (state.get("weak_claims") or []) + result.weak_claims
        ))
        return {
            "sufficient": result.sufficient,
            "gaps": result.missing_information,
            "weak_claims": merged_weak,
            "contradictory_claims": result.contradictory_claims,
        }

    return verify
```

- [ ] **Step 4: Chạy PASS**

Run: `uv run pytest tests/test_node_verify.py -v`
Expected: 2 PASS

- [ ] **Step 5: Commit**

```bash
git add app/nodes/verify.py tests/test_node_verify.py
git commit -m "feat: bounded verify node with structured output"
```

---

### Task 13: Answer node

**Files:**
- Create: `app/nodes/answer.py`
- Test: `tests/test_node_answer.py`

**Interfaces:**
- Consumes: `get_model("answer")`.
- Produces: `ANSWER_SYSTEM_PROMPT: str`; `make_answer_node(settings, model=None) -> Callable[[ResearchState], dict]` trả `{"answer": str}`. Node tự phát hiện hết-vòng: `iteration >= max_iterations and not sufficient` → chèn yêu cầu nêu uncertainty.

- [ ] **Step 1: Viết failing test tests/test_node_answer.py**

```python
from langchain_core.language_models.fake_chat_models import GenericFakeChatModel
from langchain_core.messages import AIMessage

from app.config import Settings
from app.nodes.answer import make_answer_node


def _node_with(reply: str):
    return make_answer_node(
        Settings(_env_file=None),  # type: ignore[call-arg]
        model=GenericFakeChatModel(messages=iter([AIMessage(content=reply)])),
    )


STATE = {
    "question": "Khác nhau thế nào?",
    "findings": [{"claim": "A vs B", "source_urls": ["https://a"], "confidence": "high"}],
    "sources": [{"url": "https://a", "title": "A Doc", "source_type": "primary", "excerpt": ""}],
    "sufficient": True,
    "iteration": 1,
    "max_iterations": 3,
}


def test_answer_stored_and_cited_prompt():
    captured = {}

    class Capture(GenericFakeChatModel):
        def invoke(self, messages, *a, **k):
            captured["messages"] = messages
            return super().invoke(messages, *a, **k)

    node = make_answer_node(
        Settings(_env_file=None),  # type: ignore[call-arg]
        model=Capture(messages=iter([AIMessage(content="Trả lời [1].")])),
    )
    delta = node(dict(STATE))
    assert delta["answer"] == "Trả lời [1]."
    text = str(captured["messages"])
    assert "Khác nhau thế nào?" in text
    assert "https://a" in text


def test_uncertainty_requested_when_budget_exhausted():
    captured = {}

    class Capture(GenericFakeChatModel):
        def invoke(self, messages, *a, **k):
            captured["messages"] = messages
            return super().invoke(messages, *a, **k)

    exhausted = dict(STATE, sufficient=False, iteration=3)
    node = make_answer_node(
        Settings(_env_file=None),  # type: ignore[call-arg]
        model=Capture(messages=iter([AIMessage(content="X.")])),
    )
    node(exhausted)
    assert "uncertainty" in str(captured["messages"]).lower()


def test_language_rule_present():
    node = _node_with("ok")
    assert callable(node)
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_node_answer.py -v`
Expected: FAIL — No module named app.nodes.answer

- [ ] **Step 3: Viết app/nodes/answer.py**

```python
from typing import Callable

from app.config import Settings
from app.state import ResearchState

ANSWER_SYSTEM_PROMPT = """You are WebScout's answer writer.

Write the final answer to the question using ONLY the verified findings and
sources provided. Requirements:
- Reply in the same language as the question.
- Cite claims inline as [n] where n matches the numbered source list given.
- Distinguish fact from inference. State uncertainty when present.
- Do not add unsupported claims. Do not perform new research."""


def make_answer_node(settings: Settings, model=None) -> Callable[[ResearchState], dict]:
    from app.models import get_model

    llm = model or get_model("answer", settings)

    def answer(state: ResearchState) -> dict:
        lines = [
            f"[{i}] {s.get('title', '')} — {s.get('url', '')}\n    excerpt: {s.get('excerpt', '')}"
            for i, s in enumerate(state.get("sources") or [], 1)
        ]
        sections = [
            ANSWER_SYSTEM_PROMPT,
            f"Question:\n{state['question']}",
            "Findings:\n"
            + "\n".join(
                f"- ({f.get('confidence')}) {f.get('claim')} "
                f"[refs: {', '.join(f.get('source_urls') or [])}]"
                for f in (state.get("findings") or [])
            ),
            "Numbered sources:\n" + ("\n".join(lines) or "(none)"),
        ]
        exhausted = (
            state.get("iteration", 0) >= state.get("max_iterations", 3)
            and not state.get("sufficient", False)
        )
        if exhausted:
            sections.append(
                "The verification budget was exhausted without full confidence. "
                "Explicitly state the remaining uncertainty in the answer."
            )
        reply = llm.invoke("\n\n".join(sections))
        return {"answer": str(reply.content).strip()}

    return answer
```

- [ ] **Step 4: Chạy PASS**

Run: `uv run pytest tests/test_node_answer.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add app/nodes/answer.py tests/test_node_answer.py
git commit -m "feat: bounded answer node with citations and uncertainty"
```

---

### Task 14: Graph + routing

**Files:**
- Create: `app/graph.py`
- Test: `tests/test_graph.py`

**Interfaces:**
- Consumes: 3 node factories (Tasks 11–13), `build_research_agent` (Task 8).
- Produces: `route_after_verify(state) -> "research" | "answer"`; `build_graph(settings=None) -> CompiledStateGraph` với edges START→research→verify→(conditional)→…→answer→END.

- [ ] **Step 1: Viết failing test tests/test_graph.py**

```python
import pytest

from app.graph import route_after_verify


def test_route_sufficient_goes_answer():
    assert route_after_verify({"sufficient": True}) == "answer"


def test_route_budget_left_goes_research():
    assert (
        route_after_verify({"sufficient": False, "iteration": 1, "max_iterations": 3})
        == "research"
    )


def test_route_budget_exhausted_goes_answer():
    assert (
        route_after_verify({"sufficient": False, "iteration": 3, "max_iterations": 3})
        == "answer"
    )


def test_build_graph_compiles(monkeypatch):
    import app.graph as g

    monkeypatch.setattr(g, "build_research_agent", lambda s=None: object())
    compiled = g.build_graph()
    assert compiled is not None
```

`build_graph` đọc `build_research_agent` từ namespace module nên monkeypatch có hiệu lực; không cache hàm này.

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_graph.py -v`
Expected: FAIL — No module named app.graph

- [ ] **Step 3: Viết app/graph.py**

```python
from typing import Literal

from langgraph.graph import END, START, StateGraph

from app.agent import build_research_agent
from app.config import get_settings
from app.nodes.answer import make_answer_node
from app.nodes.research import make_research_node
from app.nodes.verify import make_verify_node
from app.state import ResearchState


def route_after_verify(
    state: ResearchState,
) -> Literal["research", "answer"]:
    if state.get("sufficient"):
        return "answer"
    if state.get("iteration", 0) >= state.get("max_iterations", 3):
        return "answer"
    return "research"


def build_graph(settings=None):
    s = settings or get_settings()
    agent = build_research_agent(s)
    graph = StateGraph(ResearchState)
    graph.add_node("research", make_research_node(agent, s))
    graph.add_node("verify", make_verify_node(s))
    graph.add_node("answer", make_answer_node(s))
    graph.add_edge(START, "research")
    graph.add_edge("research", "verify")
    graph.add_conditional_edges(
        "verify",
        route_after_verify,
        {"research": "research", "answer": "answer"},
    )
    graph.add_edge("answer", END)
    return graph.compile()
```

- [ ] **Step 4: Chạy toàn bộ suite**

Run: `uv run pytest -m "not integration" -v`
Expected: tất cả PASS

- [ ] **Step 5: Commit**

```bash
git add app/graph.py tests/test_graph.py
git commit -m "feat: langgraph product loop with iteration cap"
```

---

### Task 15: CLI nâng cấp lên graph (V1)

**Files:**
- Modify: `app/main.py`
- Modify: `tests/test_cli.py`

**Interfaces:**
- Consumes: `build_graph` (Task 14). Giữ `run_question(question, agent=None)` cho tương thích V0 test; thêm `run_pipeline(question, graph=None) -> dict`.
- Produces: `run_pipeline` stream updates, in tiến trình từng node, merge delta thành final state, trả `{"answer","sources","findings","search_calls","sufficient","iteration"}`; CLI mặc định dùng pipeline.

- [ ] **Step 1: Thêm failing test vào tests/test_cli.py**

```python
class FakeGraph:
    def __init__(self, deltas):
        self._deltas = deltas

    def stream(self, state, stream_mode="updates"):
        yield from self._deltas


DELTAS = [
    {"research": {"findings": [{"claim": "c", "source_urls": [], "confidence": "high"}], "sources": [{"url": "https://s", "title": "S", "source_type": "secondary", "excerpt": ""}], "iteration": 1, "search_calls": 3, "weak_claims": []}},
    {"verify": {"sufficient": True, "gaps": [], "weak_claims": [], "contradictory_claims": []}},
    {"answer": {"answer": "Final [1]."}},
]


def test_run_pipeline_prints_progress_and_answer(capsys):
    from app.main import run_pipeline

    out = run_pipeline("Q?", graph=FakeGraph(DELTAS))
    captured = capsys.readouterr().out
    assert "research" in captured
    assert "verify" in captured
    assert "answer" in captured
    assert out["answer"] == "Final [1]."
    assert out["search_calls"] == 3
    assert out["sufficient"] is True
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_cli.py::test_run_pipeline_prints_progress_and_answer -v -m "not integration"`
Expected: FAIL — ImportError run_pipeline

- [ ] **Step 3: Sửa app/main.py**

Thêm import `from app.graph import build_graph` và hàm mới (giữ nguyên run_question):

```python
def run_pipeline(question: str, graph=None) -> dict:
    g = graph or build_graph()
    s = get_settings()
    state = {"question": question, "iteration": 0, "max_iterations": s.max_iterations}
    final = dict(state)
    for update in g.stream(state, stream_mode="updates"):
        for node, delta in update.items():
            print(f"[{node}] ...", flush=True)
            if isinstance(delta, dict):
                final.update(delta)
    return {
        "answer": final.get("answer", ""),
        "sources": final.get("sources", []),
        "findings": final.get("findings", []),
        "search_calls": final.get("search_calls", 0),
        "sufficient": final.get("sufficient", False),
        "iteration": final.get("iteration", 0),
    }
```

Đổi `_print_result(run_question(q))` thành `_print_result(run_pipeline(q))` trong `main`.

- [ ] **Step 4: Chạy toàn bộ suite**

Run: `uv run pytest -m "not integration" -v`
Expected: tất cả PASS

- [ ] **Step 5: Smoke thật khi có key**

Run: `uv run webscout "ACP và A2A khác nhau thế nào?"`
Expected: in `[research]... [verify]... [answer]...` rồi ANSWER/SOURCES.

- [ ] **Step 6: Commit**

```bash
git add app/main.py tests/test_cli.py
git commit -m "feat: v1 cli drives langgraph pipeline"
```

---

### Task 16: Acceptance V1 (9 tiêu chí spec mục 41)

Chạy tay với key thật, ghi vào `docs/superpowers/notes/v1-acceptance.md` bảng 9 tiêu chí kèm bằng chứng (link trace, output):

1. Hỏi câu factual web được → chạy `uv run webscout "..."`.
2. Có ít nhất 1 nguồn authoritative → xem SOURCES.
3. Fetch và đọc nội dung nguồn → trace LangSmith có tool call web_fetch.
4. Verifier tìm thấy thiếu bằng chứng → chọn câu hỏi khó, xem `[verify]` gaps trong trace.
5. Graph loop lại research → trace thấy research #2.
6. MAX_ITERATIONS được thi hành → đặt `max_iterations: 1` trong config.yaml tạm, chạy câu hỏi khó, xác nhận chỉ 1 vòng research rồi trả lời có uncertainty; trả config về 3.
7. Answer có source references → [n] trong answer khớp SOURCES.
8. Mọi model call qua OpenRouter → LangSmith metadata hiển thị base_url/model ox-alpha.
9. Run hiển thị LangSmith ✓.

- [ ] **Step 1..9:** thực hiện từng mục, ghi bằng chứng.
- [ ] **Step 10: Commit**

```bash
git add docs/superpowers/notes/v1-acceptance.md
git commit -m "docs: v1 acceptance against spec checklist"
```

---

### Task 17: Skill web-research (V2)

**Files:**
- Create: `skills/web-research/SKILL.md`
- Modify: `config.yaml` (bật `skills_enabled: true`)
- Test: `tests/test_skill.py`

**Interfaces:**
- Consumes: `Settings.skills_enabled`, `app/agent.py` nhánh skills đã viết sẵn (Task 8).
- Produces: SKILL.md hợp lệ Agent Skills spec; toggle hoạt động.

- [ ] **Step 1: Viết failing test tests/test_skill.py**

```python
from pathlib import Path

import yaml

ROOT = Path(__file__).parent.parent


def test_skill_md_frontmatter_valid():
    text = (ROOT / "skills" / "web-research" / "SKILL.md").read_text(encoding="utf-8")
    front = text.split("---")[1]
    meta = yaml.safe_load(front)
    assert meta["name"] == "web-research"
    assert "when" in meta["description"].lower()
    assert len(text.split("---", 2)[2]) > 500


def test_skill_body_has_methodology_sections():
    body = (ROOT / "skills" / "web-research" / "SKILL.md").read_text(encoding="utf-8")
    for heading in ("Source priority", "Verify important claims", "Handle conflicts", "Citation integrity"):
        assert heading.lower() in body.lower()
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_skill.py -v`
Expected: FAIL — FileNotFoundError

- [ ] **Step 3: Viết skills/web-research/SKILL.md**

```markdown
---
name: web-research
description: >
  Use when researching factual questions on the open web. Guides source
  selection priority, claim verification, conflict handling and citation
  integrity for every research task.
---

# Web research methodology

## Understand the question

Identify before searching: the factual claims needed, date sensitivity,
comparison dimensions, and likely authoritative sources.

## Source priority

Prefer in order:
1. official documentation
2. standards / specifications
3. government sources
4. academic papers
5. primary company sources

Use secondary sources only for interpretation, comparisons and community
views. Record which tier each key claim came from.

## Verify important claims

For important or contested claims: check multiple sources, prefer primary
evidence, record uncertainty explicitly instead of guessing.

## Track evidence

For every important finding capture: the claim, its source, publication or
update date when relevant, and a confidence level.

## Handle conflicts

If credible sources disagree: report the disagreement, never silently pick
one side, explain which source is stronger and why.

## Citation integrity

Never invent a URL, citation or source. Every [Sn] reference must correspond
to a real search result annotation or a page you fetched. If evidence cannot
be found, say so rather than filling the gap.
```

- [ ] **Step 4: Bật toggle trong config.yaml**

Sửa dòng `skills_enabled: false` → `skills_enabled: true`.

- [ ] **Step 5: Chạy suite + smoke**

Run: `uv run pytest -m "not integration" -v; uv run webscout "Ưu tiên nguồn nào khi so sánh hai framework?"`
Expected: tests PASS; agent phản ánh ưu tiên nguồn chính thống trong cách search/trả lời.

- [ ] **Step 6: Mini A/B tay (V2 acceptance)**

Với 3 câu hỏi cố định (ví dụ: "LangGraph khác Temporal?", "ACP vs A2A?", "trafilatura hỗ trợ tiếng Việt không?"): chạy 2 lượt — lượt 1 `skills_enabled: true`, lượt 2 `false`. Lưu 6 output vào `evals/runs/skill-ab-v2.md` kèm nhận xét nhanh về chất lượng nguồn/citations.

```bash
git add skills/web-research/SKILL.md config.yaml tests/test_skill.py evals/runs/skill-ab-v2.md
git commit -m "feat: web-research skill with ab comparison notes"
```

---

### Task 18: Eval dataset + evaluators

**Files:**
- Create: `evals/dataset.json`, `evals/evaluators.py`
- Test: `tests/test_evaluators.py`

**Interfaces:**
- Consumes: `get_model("verifier")` làm judge.
- Produces:
  - `dataset.json`: list object `{"id", "category", "question", "reference_notes"}` — 24 câu phủ 8 category của spec mục 24.
  - `correctness_evaluator(run, example) -> EvaluationResult` — LLM judge chấm 1-5 so với reference_notes.
  - `citation_support_evaluator(run, example) -> EvaluationResult` — kiểm tra mọi [n] trong answer resolve được + judge spot-check 1 claim vs excerpt.
  - `metrics_evaluator(run, example) -> list[EvaluationResult]` — deterministic: latency_s, total_tokens, search_calls (từ outputs), num_sources.

- [ ] **Step 1: Viết evals/dataset.json — 24 câu thật**

8 nhóm × 3 câu, xen kẽ Việt/Anh, ví dụ seed (executor hoàn chỉnh đủ 24 theo mẫu, giữ đúng schema):

```json
[
  {"id": "tech-current-1", "category": "current technology", "question": "Phiên bản ổn định mới nhất của LangGraph là gì và ngày phát hành?", "reference_notes": "Cần phiên bản cụ thể + ngày từ PyPI/GitHub releases."},
  {"id": "compare-1", "category": "product comparison", "question": "LangGraph khác Temporal ở mô hình thực thi?", "reference_notes": "State-machine orchestration vs durable workflow execution; replay semantics."},
  {"id": "history-1", "category": "historical fact", "question": "HTTP/2 được chuẩn hóa năm nào bởi IETF?", "reference_notes": "RFC 7540, tháng 5/2015."},
  {"id": "doc-1", "category": "technical documentation", "question": "trafilatura có tham số nào để giữ link khi trích xuất?", "reference_notes": "include_links trong extract(); kiểm tra docs."},
  {"id": "science-1", "category": "scientific question", "question": "CRISPR-Cas9 phân biệt target DNA nhờ gì?", "reference_notes": "guide RNA complementarity + PAM sequence."},
  {"id": "synthesis-1", "category": "multi-source synthesis", "question": "Ưu nhược điểm của uv so với pip+venv cho dự án Python vừa?", "reference_notes": "Tốc độ lockfile vs độ phổ biến/tương thích ecosystem."},
  {"id": "conflict-1", "category": "conflicting evidence", "question": "Python 3.13 free-threading có ảnh hưởng hiệu năng single-thread không? Các nguồn nói gì?", "reference_notes": "Kỳ vọng: overhead nhỏ ~vài %; phải nêu nếu nguồn mâu thuẫn."},
  {"id": "news-1", "category": "recent news", "question": "OpenRouter có tính năng server tools nào phát hành gần nhất?", "reference_notes": "openrouter:web_search server tool; so tài liệu hiện hành."}
]
```

Executor viết đủ 24 object theo đúng schema trên — mỗi category đủ 3 câu (`*-2`, `*-3`), câu hỏi thật kiểm chứng được, không bỏ trống trường nào.

- [ ] **Step 2: Viết failing test tests/test_evaluators.py**

```python
from types import SimpleNamespace

from evals.evaluators import (
    citation_support_evaluator,
    metrics_evaluator,
)


class FakeRun(SimpleNamespace):
    pass


def _run(answer, sources, searches=2, tokens=1234, seconds=9.0):
    start = __import__("datetime").datetime(2026, 1, 1, 0, 0, 0)
    return FakeRun(
        outputs={"answer": answer, "sources": sources, "search_calls": searches},
        total_tokens=tokens,
        start_time=start,
        end_time=__import__("datetime").datetime(2026, 1, 1, 0, 0, seconds),
        error=None,
    )


EXAMPLE = SimpleNamespace(outputs={"reference_notes": "note"})


def test_metrics_evaluator_values():
    results = metrics_evaluator(
        _run("a [1]", [{"url": "https://x"}], searches=4, tokens=999, seconds=12.0),
        EXAMPLE,
    )
    kv = {r.key: r.value for r in results}
    assert kv["latency_s"] == 12.0
    assert kv["total_tokens"] == 999
    assert kv["search_calls"] == 4
    assert kv["num_sources"] == 1


def test_citation_support_flags_unresolved_refs():
    result = citation_support_evaluator(
        _run("claim [3]", [{"url": "https://x"}]), EXAMPLE
    )
    assert result.score <= 0.5


def test_citation_support_passes_resolved():
    result = citation_support_evaluator(
        _run("claim [1] done", [{"url": "https://x", "title": "X", "excerpt": "claim"}]),
        EXAMPLE,
    )
    assert result.score >= 0.5
```

- [ ] **Step 3: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_evaluators.py -v`
Expected: FAIL — No module named evals.evaluators (thêm `evals/__init__.py` rỗng nếu import gói)

- [ ] **Step 4: Viết evals/evaluators.py**

```python
import json
import re

from langsmith.evaluation import EvaluationResult

from app.models import get_model

_CITE_RE = re.compile(r"\[(\d+)\]")


def correctness_evaluator(run, example) -> EvaluationResult:
    question = (run.inputs or {}).get("question", "")
    answer = (run.outputs or {}).get("answer", "")
    notes = (example.outputs or {}).get("reference_notes", "")
    judge = get_model("verifier")
    verdict = judge.invoke(
        [
            (
                "system",
                "You grade research answers. Return JSON only: "
                '{"score": <1-5>, "reason": "<short>"} where 5 means fully '
                "correct, well-scoped and honest about uncertainty.",
            ),
            (
                "human",
                f"Question:\n{question}\n\nReference notes:\n{notes}\n\n"
                f"Answer:\n{answer}",
            ),
        ]
    )
    try:
        parsed = json.loads(re.search(r"\{[\s\S]*\}", str(verdict.content)).group(0))
        score = float(parsed.get("score", 0)) / 5.0
        reason = str(parsed.get("reason", ""))
    except Exception as exc:
        score, reason = 0.0, f"judge-parse-error: {exc}"
    return EvaluationResult(key="correctness", score=score, comment=reason)


def citation_support_evaluator(run, example) -> EvaluationResult:
    outputs = run.outputs or {}
    answer = outputs.get("answer", "")
    sources = outputs.get("sources") or []
    refs = {int(n) for n in _CITE_RE.findall(answer)}
    unresolved = [n for n in refs if n < 1 or n > len(sources)]
    if not refs or unresolved:
        base = 0.0
    else:
        base = 1.0
    return EvaluationResult(
        key="citation_support",
        score=base,
        comment=f"unresolved={unresolved}, refs={sorted(refs)}",
    )


def metrics_evaluator(run, example) -> list[EvaluationResult]:
    outputs = run.outputs or {}
    latency = 0.0
    if getattr(run, "start_time", None) and getattr(run, "end_time", None):
        latency = (run.end_time - run.start_time).total_seconds()
    values = {
        "latency_s": latency,
        "total_tokens": int(getattr(run, "total_tokens", 0) or 0),
        "search_calls": int(outputs.get("search_calls", 0) or 0),
        "num_sources": len(outputs.get("sources") or []),
    }
    return [EvaluationResult(key=k, score=float(v)) for k, v in values.items()]
```

Ghi chú: evaluator là hàm thuần trả `EvaluationResult`/list — `langsmith.evaluate()` nhận trực tiếp, không cần decorator; chữ ký `(run, example)` là chuẩn của langsmith.

- [ ] **Step 5: Chạy PASS**

Run: `uv run pytest tests/test_evaluators.py -v`
Expected: 3 PASS

- [ ] **Step 6: Commit**

```bash
git add evals/dataset.json evals/evaluators.py tests/test_evaluators.py
git commit -m "feat: eval dataset and evaluators"
```

---

### Task 19: Eval runner + experiment (V3)

**Files:**
- Create: `evals/run_evals.py`
- Test: `tests/test_runner_unit.py`

**Interfaces:**
- Consumes: `build_graph`, evaluators (Task 18), dataset.json.
- Produces: `load_dataset() -> list[dict]`; `target(inputs: dict) -> dict` chạy full pipeline trả `{"answer","sources","search_calls"}`; `main(--experiment-prefix, --limit)` upload dataset lên LangSmith (nếu chưa có) rồi `evaluate(...)`; CLI: `uv run python -m evals.run_evals --experiment-prefix ab-skill-on`.

- [ ] **Step 1: Viết failing test tests/test_runner_unit.py**

```python
from unittest.mock import patch

from evals.run_evals import load_dataset, target


def test_load_dataset_shape():
    rows = load_dataset()
    assert len(rows) >= 20
    ids = [r["id"] for r in rows]
    assert len(ids) == len(set(ids))
    assert all({"id", "category", "question", "reference_notes"} <= set(r) for r in rows)


def test_target_maps_outputs(monkeypatch):
    class FakeCompiled:
        def invoke(self, state):
            return {"answer": "A", "sources": [{"url": "u"}], "search_calls": 5}

    import evals.run_evals as mod

    monkeypatch.setattr(mod, "_graph", lambda: FakeCompiled())
    out = target({"question": "q"})
    assert out == {"answer": "A", "sources": [{"url": "u"}], "search_calls": 5}
```

- [ ] **Step 2: Chạy xác nhận FAIL**

Run: `uv run pytest tests/test_runner_unit.py -v`
Expected: FAIL — No module named evals.run_evals

- [ ] **Step 3: Viết evals/run_evals.py**

```python
import argparse
import json
from functools import lru_cache
from pathlib import Path

from langsmith import Client

DATASET_PATH = Path(__file__).parent / "dataset.json"
DATASET_NAME = "webscout-evals-v1"


def load_dataset() -> list[dict]:
    return json.loads(DATASET_PATH.read_text(encoding="utf-8"))


@lru_cache(maxsize=1)
def _graph():
    from app.graph import build_graph

    return build_graph()


def target(inputs: dict) -> dict:
    s = _graph().invoke(
        {
            "question": inputs["question"],
            "iteration": 0,
            "max_iterations": 3,
        }
    )
    return {
        "answer": s.get("answer", ""),
        "sources": s.get("sources", []),
        "search_calls": s.get("search_calls", 0),
    }


def ensure_dataset(client: Client):
    if client.has_dataset(dataset_name=DATASET_NAME):
        return client.read_dataset(dataset_name=DATASET_NAME)
    ds = client.create_dataset(dataset_name=DATASET_NAME)
    client.create_examples(
        dataset_id=ds.id,
        inputs=[{"question": r["question"]} for r in load_dataset()],
        outputs=[{"reference_notes": r["reference_notes"]} for r in load_dataset()],
    )
    return ds


def main(argv=None) -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--experiment-prefix", default="webscout-run")
    parser.add_argument("--limit", type=int, default=0)
    args = parser.parse_args(argv)

    from evals.evaluators import (
        citation_support_evaluator,
        correctness_evaluator,
        metrics_evaluator,
    )

    client = Client()
    ensure_dataset(client)
    rows = load_dataset()
    examples = rows[: args.limit] if args.limit else rows
    client.evaluate(
        target,
        data=[
            {"inputs": {"question": r["question"]}, "outputs": {"reference_notes": r["reference_notes"]}}
            for r in examples
        ],
        evaluators=[
            correctness_evaluator,
            citation_support_evaluator,
            metrics_evaluator,
        ],
        experiment_prefix=args.experiment_prefix,
        max_concurrency=2,
        dataset_name=DATASET_NAME,
    )


if __name__ == "__main__":
    main()
```

Nếu chữ ký `client.evaluate(...)` khác giữa các phiên bản langsmith: fallback dùng `langsmith.evaluate(target, data=..., evaluators=..., dataset_name=...)` hàm bậc cao; executor điều chỉnh import tối thiểu, GIỮ nguyên tên hàm/tham số chính.

- [ ] **Step 4: Chạy PASS unit**

Run: `uv run pytest tests/test_runner_unit.py -v`
Expected: 2 PASS

- [ ] **Step 5: Experiment thật (có key) — ma trận theo spec mục 10**

Chạy đủ tổ hợp `skills_enabled × max_iterations` (đọc/toggle trong config.yaml giữa các lượt, luôn trả về giá trị gốc sau mỗi lượt):

```powershell
uv run python -m evals.run_evals --experiment-prefix skill-off-iters3 --limit 6
# toggle skills_enabled: true
uv run python -m evals.run_evals --experiment-prefix skill-on-iters3 --limit 6
# toggle skills_enabled: false; sửa max_iterations: 1
uv run python -m evals.run_evals --experiment-prefix skill-off-iters1 --limit 6
# toggle skills_enabled: true
uv run python -m evals.run_evals --experiment-prefix skill-on-iters1 --limit 6
# trả config gốc: skills_enabled + max_iterations như trước khi thử nghiệm
```

So 4 experiments trên LangSmith UI: correctness trung bình, search_calls, latency — kết luận ghi vào `docs/superpowers/notes/v3-experiments.md`.

- [ ] **Step 6: Commit**

```bash
git add evals/run_evals.py tests/test_runner_unit.py
git commit -m "feat: langsmith eval runner and experiment flow"
```

---

### Task 20: README + polish cuối

**Files:**
- Create: `README.md`

- [ ] **Step 1: Viết README.md**

Nội dung bắt buộc (viết đầy đủ, không placeholder): giới thiệu 2 câu; sơ đồ loop text; setup (`uv sync --extra dev`, copy `.env.example` → `.env`, điền key, `LANGSMITH_*` tuỳ chọn); usage CLI one-shot + interactive; chạy test (`uv run pytest -m "not integration"`, integration cần key); evals (`uv run python -m evals.run_evals`); bảng config config.yaml; link tới spec + notes.

- [ ] **Step 2: Full suite xanh cuối cùng**

Run: `uv run pytest -m "not integration" -v; git status --short`
Expected: tất cả PASS; working tree sạch (trừ README staged).

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: readme with setup, usage, testing, evals"
```

---

## Phụ lục: xử lý sự kiện thường gặp

- **`YamlConfigSettingsSource` không tồn tại**: nâng `pydantic-settings[yaml]>=2.6`; vẫn lỗi thì fallback đọc yaml thủ công trong `settings_customise_sources` thay bằng post-load merge trong `get_settings()` (document lựa chọn vào commit message).
- **deepagents API lệch** (ví dụ `create_deep_agent` không nhận `skills`): đọc `uv run python -c "import deepagents, inspect; print(inspect.signature(deepagents.create_deep_agent))"` và điều chỉnh kwargs; nguyên tắc: KHÔNG fork harness, chỉ đổi cách truyền.
- **Verifier JSON hỏng** (`_parse_verdict` raise): retry một lần với nhắc lại format; vẫn fail thì coi `sufficient=False` + gaps=["verifier parse error"] để loop an toàn, log warning.
- **Windows console Unicode**: nếu `print` lỗi cp65001, đặt `$env:PYTHONIOENCODING="utf-8"` đầu PowerShell session.
